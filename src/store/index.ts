/**
 * Application state management (Redux Toolkit or Zustand).
 *
 * Redux Toolkit:
 *   - Add: store/index.ts (configureStore), store/slices/, store/hooks.ts (useAppDispatch, useAppSelector)
 *   - Wrap app with Provider from react-redux
 *
 * Zustand:
 *   - Add: store/slices/*.ts (create(...)) and re-export from here
 */

export {};
